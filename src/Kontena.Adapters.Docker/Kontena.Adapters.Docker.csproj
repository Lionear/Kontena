<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\Kontena.Sdk\Kontena.Sdk.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Docker.DotNet" Version="3.125.15" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- The browse-path rule keeps a listing inside the mount; it is an implementation detail, and the
         one that matters if it is wrong, so the tests reach it here rather than by widening it. -->
    <InternalsVisibleTo Include="Kontena.Adapters.Docker.Tests" />
  </ItemGroup>

</Project>
