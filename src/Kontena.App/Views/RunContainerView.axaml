<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:Kontena.App.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="660" d:DesignHeight="720"
             x:Class="Kontena.App.Views.RunContainerView"
             x:DataType="vm:RunContainerViewModel"
             x:Name="DialogRoot">

    <UserControl.Styles>
        <Style Selector="TextBox.fieldinput">
            <Setter Property="Background" Value="{DynamicResource Bg}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Border}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="MinHeight" Value="0"/>
            <Setter Property="FontFamily" Value="{DynamicResource Mono}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="{DynamicResource Text}"/>
            <Setter Property="CaretBrush" Value="{DynamicResource Primary}"/>
        </Style>
        <Style Selector="TextBox.fieldinput:focus /template/ Border#PART_BorderElement">
            <Setter Property="BorderBrush" Value="{DynamicResource Primary}"/>
        </Style>
        <Style Selector="ComboBox.fieldinput">
            <Setter Property="Background" Value="{DynamicResource Bg}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Border}"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="10,7"/>
            <Setter Property="FontFamily" Value="{DynamicResource Mono}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
        </Style>
        <Style Selector="TextBlock.flabel">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource TextDim}"/>
            <Setter Property="Margin" Value="0,0,0,7"/>
        </Style>
        <Style Selector="Button.del">
            <Setter Property="Background" Value="{DynamicResource Bg}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Border}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="7"/>
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="0"/>
        </Style>
        <Style Selector="Button.del:pointerover /template/ ContentPresenter">
            <Setter Property="TextElement.Foreground" Value="{DynamicResource Danger}"/>
        </Style>
        <Style Selector="Button.addrow">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="{DynamicResource PrimaryDim}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="8,7"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="{DynamicResource Primary}"/>
            <Setter Property="FontSize" Value="12.5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,9,0,0"/>
        </Style>
    </UserControl.Styles>

    <Border Width="660" MaxHeight="760" Background="{DynamicResource Surface}" BorderBrush="{DynamicResource Border}"
            BorderThickness="1" CornerRadius="14" ClipToBounds="True" BoxShadow="0 32 80 0 #99000000">
        <Grid RowDefinitions="Auto,*,Auto">

            <!-- Header -->
            <Grid Grid.Row="0" ColumnDefinitions="Auto,*,Auto" Margin="24,20"
                  Background="Transparent">
                <Border Grid.Column="0" Width="36" Height="36" CornerRadius="9" Background="{DynamicResource PrimarySoft}">
                    <Path Classes="icon" Width="19" Height="19" Stroke="{DynamicResource Primary}" Data="{StaticResource IconPlay}"/>
                </Border>
                <StackPanel Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center" Spacing="2">
                    <TextBlock Text="Run a container" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource Text}"/>
                    <TextBlock Text="Configure and launch — Kontena builds the run for the active engine"
                               FontSize="12.5" Foreground="{DynamicResource TextFaint}"/>
                </StackPanel>
                <Button Grid.Column="2" Classes="qa" Width="32" Height="32" Command="{Binding CancelCommand}" ToolTip.Tip="Close">
                    <Path Classes="icon" Width="15" Height="15" Stroke="{DynamicResource TextDim}" Data="{StaticResource IconClose}"/>
                </Button>
            </Grid>

            <Border Grid.Row="0" Height="1" Background="{DynamicResource Border}" VerticalAlignment="Bottom"/>

            <!-- Body -->
            <ScrollViewer Grid.Row="1">
                <StackPanel Margin="24,20" Spacing="18">

                    <!-- Image -->
                    <StackPanel>
                        <TextBlock Classes="flabel" Text="Image"/>
                        <Grid ColumnDefinitions="*,Auto">
                            <TextBox Grid.Column="0" Classes="fieldinput" Text="{Binding Image, Mode=TwoWay}" PlaceholderText="e.g. postgres:16"/>
                            <Button Grid.Column="1" Classes="ghost" Padding="12,8" Margin="8,0,0,0"
                                    Command="{Binding PullCommand}" IsEnabled="{Binding CanPull}" IsVisible="{Binding IsImagePulled}"
                                    ToolTip.Tip="Pull now to auto-fill ports and volumes">
                                <StackPanel Orientation="Horizontal" Spacing="7">
                                    <Path Classes="icon" Width="14" Height="14" Stroke="{DynamicResource TextDim}" Data="{StaticResource IconPull}"/>
                                    <TextBlock Text="Pull" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                        </Grid>
                        <StackPanel Orientation="Horizontal" Spacing="8" Margin="2,8,0,0">
                            <Border CornerRadius="6" Padding="9,4" Background="#34D39916" IsVisible="{Binding IsImageLocal}">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <Ellipse Width="6" Height="6" Fill="{DynamicResource Success}" VerticalAlignment="Center"/>
                                    <TextBlock Text="Found locally" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource Success}"/>
                                </StackPanel>
                            </Border>
                            <Border CornerRadius="6" Padding="9,4" Background="#5AB8FF16"
                                    IsVisible="{Binding IsImagePulled}" IsEnabled="{Binding !IsPulling}">
                                <TextBlock Text="Will be pulled before start" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource Info}"/>
                            </Border>
                            <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center" IsVisible="{Binding IsPulling}">
                                <ProgressBar IsIndeterminate="True" Width="70" Height="3"
                                             Foreground="{DynamicResource Primary}" Background="{DynamicResource Surface2}"/>
                                <TextBlock Text="{Binding PullStatus}" FontSize="11.5" FontFamily="{StaticResource Mono}"
                                           Foreground="{DynamicResource TextDim}"/>
                            </StackPanel>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Spacing="7" Margin="2,8,0,0" IsVisible="{Binding ShowPullHint}">
                            <Path Classes="icon" Width="13" Height="13" Stroke="{DynamicResource TextFaint}"
                                  Data="{StaticResource IconInfo}" VerticalAlignment="Top" Margin="0,2,0,0"/>
                            <TextBlock FontSize="11.5" Foreground="{DynamicResource TextFaint}" TextWrapping="Wrap" MaxWidth="560"
                                       Text="Pull now to let Kontena read the image and auto-fill its ports and volumes. Otherwise it's pulled automatically when you run."/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Name + Restart -->
                    <Grid ColumnDefinitions="*,*" ColumnSpacing="14">
                        <StackPanel Grid.Column="0">
                            <TextBlock Classes="flabel" Text="Container name  (optional)"/>
                            <TextBox Classes="fieldinput" Text="{Binding ContainerName, Mode=TwoWay}" PlaceholderText="auto-generated"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock Classes="flabel" Text="Restart policy"/>
                            <ComboBox Classes="fieldinput" ItemsSource="{Binding RestartPolicies}"
                                      SelectedItem="{Binding SelectedRestartPolicy, Mode=TwoWay}"/>
                        </StackPanel>
                    </Grid>

                    <!-- Ports -->
                    <StackPanel>
                        <TextBlock Classes="flabel" Text="Port mappings  (host → container)"/>
                        <ItemsControl ItemsSource="{Binding Ports}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate x:DataType="vm:PortRow">
                                    <Grid ColumnDefinitions="*,22,*,Auto" Margin="0,0,0,8">
                                        <TextBox Grid.Column="0" Classes="fieldinput" Text="{Binding Host, Mode=TwoWay}" PlaceholderText="host"/>
                                        <TextBlock Grid.Column="1" Text="→" Foreground="{DynamicResource TextFaint}" FontFamily="{StaticResource Mono}"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        <TextBox Grid.Column="2" Classes="fieldinput" Text="{Binding Container, Mode=TwoWay}" PlaceholderText="container e.g. 5432/tcp"/>
                                        <Button Grid.Column="3" Classes="del" Margin="8,0,0,0"
                                                Command="{Binding #DialogRoot.((vm:RunContainerViewModel)DataContext).RemovePortCommand}"
                                                CommandParameter="{Binding}">
                                            <Path Classes="icon" Width="14" Height="14" Stroke="{DynamicResource TextFaint}" Data="{StaticResource IconTrash}"/>
                                        </Button>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <Button Classes="addrow" Command="{Binding AddPortCommand}">
                            <StackPanel Orientation="Horizontal" Spacing="7">
                                <Path Classes="icon" Width="14" Height="14" Stroke="{DynamicResource Primary}" Data="{StaticResource IconPlus}"/>
                                <TextBlock Text="Add port" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>

                    <!-- Environment -->
                    <StackPanel>
                        <TextBlock Classes="flabel" Text="Environment variables"/>
                        <Border IsVisible="{Binding HasRecipe}" Background="{DynamicResource PrimarySoft}" CornerRadius="7"
                                Padding="10,6" Margin="0,0,0,8">
                            <StackPanel Orientation="Horizontal" Spacing="7">
                                <Path Classes="icon" Width="13" Height="13" Stroke="{DynamicResource Primary}" Data="{StaticResource IconInfo}"
                                      VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding RecipeHint}" FontSize="12" Foreground="{DynamicResource Primary}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <ItemsControl ItemsSource="{Binding EnvVars}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate x:DataType="vm:EnvRow">
                                    <Grid ColumnDefinitions="10,*,22,*,Auto" Margin="0,0,0,8">
                                        <Ellipse Grid.Column="0" Width="7" Height="7" Fill="{DynamicResource Danger}"
                                                 VerticalAlignment="Center" HorizontalAlignment="Left"
                                                 IsVisible="{Binding IsRequired}" ToolTip.Tip="Required"/>
                                        <TextBox Grid.Column="1" Classes="fieldinput" Text="{Binding Key, Mode=TwoWay}" PlaceholderText="KEY"/>
                                        <TextBlock Grid.Column="2" Text="=" Foreground="{DynamicResource TextFaint}" FontFamily="{StaticResource Mono}"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        <TextBox Grid.Column="3" Classes="fieldinput" Text="{Binding Value, Mode=TwoWay}"
                                                 PlaceholderText="{Binding ValuePlaceholder}"/>
                                        <Button Grid.Column="4" Classes="del" Margin="8,0,0,0"
                                                Command="{Binding #DialogRoot.((vm:RunContainerViewModel)DataContext).RemoveEnvCommand}"
                                                CommandParameter="{Binding}">
                                            <Path Classes="icon" Width="14" Height="14" Stroke="{DynamicResource TextFaint}" Data="{StaticResource IconTrash}"/>
                                        </Button>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <Button Classes="addrow" Command="{Binding AddEnvCommand}">
                            <StackPanel Orientation="Horizontal" Spacing="7">
                                <Path Classes="icon" Width="14" Height="14" Stroke="{DynamicResource Primary}" Data="{StaticResource IconPlus}"/>
                                <TextBlock Text="Add variable" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>

                    <!-- Volumes -->
                    <StackPanel>
                        <TextBlock Classes="flabel" Text="Volume mounts  (source → container path)"/>
                        <ItemsControl ItemsSource="{Binding Volumes}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate x:DataType="vm:VolumeRow">
                                    <Grid ColumnDefinitions="*,22,*,Auto" Margin="0,0,0,8">
                                        <TextBox Grid.Column="0" Classes="fieldinput" Text="{Binding Source, Mode=TwoWay}" PlaceholderText="volume or host path"/>
                                        <TextBlock Grid.Column="1" Text="→" Foreground="{DynamicResource TextFaint}" FontFamily="{StaticResource Mono}"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        <TextBox Grid.Column="2" Classes="fieldinput" Text="{Binding Destination, Mode=TwoWay}" PlaceholderText="/path/in/container"/>
                                        <Button Grid.Column="3" Classes="del" Margin="8,0,0,0"
                                                Command="{Binding #DialogRoot.((vm:RunContainerViewModel)DataContext).RemoveVolumeCommand}"
                                                CommandParameter="{Binding}">
                                            <Path Classes="icon" Width="14" Height="14" Stroke="{DynamicResource TextFaint}" Data="{StaticResource IconTrash}"/>
                                        </Button>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <Button Classes="addrow" Command="{Binding AddVolumeCommand}">
                            <StackPanel Orientation="Horizontal" Spacing="7">
                                <Path Classes="icon" Width="14" Height="14" Stroke="{DynamicResource Primary}" Data="{StaticResource IconPlus}"/>
                                <TextBlock Text="Add volume" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>

                    <!-- Network -->
                    <StackPanel>
                        <TextBlock Classes="flabel" Text="Network"/>
                        <ComboBox Classes="fieldinput" ItemsSource="{Binding Networks}"
                                  SelectedItem="{Binding SelectedNetwork, Mode=TwoWay}"/>
                    </StackPanel>

                </StackPanel>
            </ScrollViewer>

            <!-- Footer -->
            <StackPanel Grid.Row="2" Background="#12161D">
                <Border Height="1" Background="{DynamicResource Border}"/>
                <Grid ColumnDefinitions="Auto,*" Margin="24,12" MinHeight="20">
                    <TextBlock Grid.Column="0" Text="preview" FontFamily="{StaticResource Mono}" FontSize="12"
                               Foreground="{DynamicResource TextFaint}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBlock Grid.Column="1" Text="{Binding CommandPreview}" FontFamily="{StaticResource Mono}" FontSize="12"
                               Foreground="{DynamicResource TextDim}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                </Grid>
                <Border Height="1" Background="{DynamicResource Border}"/>

                <TextBlock Text="{Binding Error}" IsVisible="{Binding Error, Converter={x:Static ObjectConverters.IsNotNull}}"
                           Foreground="{DynamicResource Danger}" FontSize="12.5" Margin="24,12,24,0" TextWrapping="Wrap"/>

                <StackPanel Orientation="Horizontal" Spacing="7" Margin="24,12,24,0"
                            IsVisible="{Binding RunBlockReason, Converter={x:Static ObjectConverters.IsNotNull}}">
                    <Path Classes="icon" Width="13" Height="13" Stroke="{DynamicResource Warn}" Data="{StaticResource IconInfo}"
                          VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding RunBlockReason}" Foreground="{DynamicResource Warn}" FontSize="12.5"
                               VerticalAlignment="Center" TextWrapping="Wrap"/>
                </StackPanel>

                <Grid ColumnDefinitions="*,Auto" Margin="24,16">
                    <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="9" VerticalAlignment="Center">
                        <Border Width="20" Height="20" CornerRadius="5" Background="{DynamicResource DockerChip}">
                            <TextBlock Text="{Binding BackendChip}" FontFamily="{StaticResource Mono}" FontWeight="Bold" FontSize="10"
                                       Foreground="{DynamicResource Info}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock FontSize="12.5" Foreground="{DynamicResource TextDim}" VerticalAlignment="Center">
                            <Run Text="Running on"/><Run Text=" "/><Run Text="{Binding BackendName}" FontWeight="SemiBold"/>
                        </TextBlock>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="10">
                        <Button Classes="ghost" Command="{Binding CancelCommand}">Cancel</Button>
                        <Button Classes="primary" Command="{Binding RunCommand}" IsEnabled="{Binding CanRun}">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <Path Classes="icon" Width="15" Height="15" Stroke="{DynamicResource PrimaryInk}" Data="{StaticResource IconPlay}"/>
                                <TextBlock Text="Run container" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
