- **The first Back of a session works.** Opening anything from the page Kontena starts on left the
  top bar without a Back button — and the keyboard shortcut and mouse button with nothing to do —
  because the page you started on was never recorded as somewhere you had been. From the second
  navigation onwards it behaved, which is what made it look like the button was failing to draw
  rather than the history missing an entry. Clusters had the same gap on their overview page.
