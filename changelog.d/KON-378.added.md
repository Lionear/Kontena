- **See what a cluster can hold, not only what is on it.** Cluster ▸ Overview gains **Max CPUs** and
  **Max Memory** tiles — the allocatable capacity of every Ready node added up — and the node table
  there gains a **MEMORY** column beside CPU, in the same "used / capacity" form.
