- **Every cluster list keeps up with the cluster, not just three of them.** Workloads, pods and
  services followed the cluster; nodes, namespaces, ingresses, volume claims, volumes and storage
  classes still showed whatever was true when you opened them. All nine now update themselves, and
  all nine say so when they cannot — a list that has quietly stopped moving looks exactly like a
  cluster where nothing is happening, and those two want opposite reactions. Ingresses, volume
  claims, volumes and storage classes also needed the watching to exist on the Kubernetes side at
  all, so that was added rather than letting those pages blame the cluster for a stream Kontena
  never opened.
