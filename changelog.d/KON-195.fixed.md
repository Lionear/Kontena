- **Double-clicking the title bar maximises and restores the window again.** It was left to the
  platform, which never gets the chance: Kontena draws its own title bar, so the window manager sees an
  ordinary click and does nothing with it. Two quick clicks on the minimise, maximise or close button
  still only do what that button says.
